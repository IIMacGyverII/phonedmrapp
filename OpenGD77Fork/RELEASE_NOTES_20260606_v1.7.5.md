# OpenGD77 CPS — PriInterPhone fork v1.7.5

**Date:** June 6, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260606_124728.zip`

## Navigation tree filter

- **Filter box** at the top of the left tree panel — type to narrow zones, channels, contacts, TG lists, and other nodes by name.
- Non-matching branches are hidden; matching paths auto-expand.
- Filter is preserved across tree rebuilds (channel add/delete, import, etc.).

## Codeplug health (F7) — additional checks

- **Duplicate contact names** — same display name on multiple contacts (status bar: `dup ct nm`).
- **Digital channels without TX contact** — digital mode channels with no contact assigned (`dig no ct`).
- F7 HTML report and status bar include both new warning types.

`FORK_VERSION` = **1.7.5**