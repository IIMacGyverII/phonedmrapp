# OpenGD77 CPS — PriInterPhone fork v1.5.4

**Date:** June 5, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260605_203007.zip`

## Tier 1.5 — Default advanced fields on

- **Show advanced codeplug fields** now defaults to **checked** for new installs (no `ChannelShowAdvancedBinary` in `Setup.ini` yet).
- Existing saved preference (`yes` / `no`) is unchanged.

`FORK_VERSION` = **1.5.4**