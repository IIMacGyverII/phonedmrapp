# phonedmrapp — Agent Instructions

**Read `.grok/rules/00-session-start.md` first on every new session** before editing code, building, or deploying.

Full project reference: `.grok/rules/copilot-instructions.md` (clone of `.github/copilot-instructions.md`).

Dead-end history (do not re-investigate without new evidence): `.docs/AI_LOGS_SUMMARY.md`.

OpenGD77 CPS fork: `OpenGD77Fork/` (build zips) + source at `OpenGD77CPS-Mac` — see `.grok/rules/00-session-start.md` §8.